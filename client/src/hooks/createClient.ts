export type player = {
    name: string;
    };



export function createClient(name: string): player {
    // This is a stub, you can implement your own logic here
    return {
        name: name,
    }
}
